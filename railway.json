{
  "buildCommand": "echo 'Build Laravel Project'",
  "startCommand": "php artisan serve --host=0.0.0.0 --port=8080",
  "preDeployCommand": [
    "npm install",
    "npm run build",
    "php artisan migrate:fresh --force"
  ]
}
